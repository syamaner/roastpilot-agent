# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/

# Tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
coverage.json
coverage.xml
htmlcov/

# Environment / secrets
.env
.env.*

# Local data — roast logs and SQLite databases never go in git
logs/
*.sqlite3
*.sqlite3-wal
*.sqlite3-shm
*.db

# Audio, model weights, serial captures (AGENTS.md: never committed)
*.wav
*.mp3
*.flac
*.onnx
*.pt
*.safetensors
*.pcap
serial-captures/

# uv (project is pip-based; un-ignore if uv is ever adopted)
uv.lock

# Node / SPA (web/ arrives at E10)
node_modules/
web/dist/

# IDE / OS
.idea/
.vscode/
.DS_Store

# Derived Artisan bake-off fixtures (personal roast logs — not committed, AGENTS.md)
.artisan-fixtures/
.artisan-holdout/

# RP-B (#709) ambient-doctrine eval fixtures (D129) — personal roast logs, plus
# the operator's own name-to-run-id MAPPING.local.md, neither ever committed
# (mirrors .artisan-fixtures/ — see scripts/rp_b_ambient_fixtures.py).
.rp-b-fixtures/

# Store-sourced replay fixtures (#224/D44), written by scripts/store_to_fixture.py
# — personal roast telemetry, never committed; only the manifest + report are
# (mirrors .artisan-fixtures/). Added 11 Aug 2026: the manifest's own note has
# asserted "gitignored under .store-fixtures/" since June, but no rule existed, so
# the protection was claimed and not enforced — one `git add -A` from committing
# the operator's roast logs.
.store-fixtures/

# Opt-in agent-usage pilot: local metadata only, never commit the append-only sink.
.agent-usage/

# Large per-roast bake-off dumps (regenerable; commit the summary doc instead)
docs/advisor/bakeoff-results-2026-06-16.json
docs/advisor/bakeoff-results-2026-06-16.md

# Bake-off per-call capture (#284): full prompts + responses + reasoning traces.
# Large + contains raw advisor I/O — only the derived summary / report is
# committed, never the raw capture (mirrors the .artisan-fixtures convention).
*.capture.jsonl

# design-sync (claude.ai/design) — staged scripts, build output, machine state
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules

# Playwright MCP interactive-session artifacts (ui-reviewer captures — never committed)
.playwright-mcp/

# Phase-1 plant-model study: raw per-tick roast traces are never committed (AGENTS.md).
step_response_traces.csv
docs/research/plant-model/step_response_traces.csv
